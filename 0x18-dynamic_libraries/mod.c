#include "operations.h"
/**
 *mod - returns the reminder of the division of two integers
 *
 *@a: the first int
 *@b: the second int
 *
 * Return: integer
 */
int mod(int a, int b)
{
	return (a % b);
}
