#include "main.h"
#include <stdio.h>
/**
 *main - Entry point
 *@argc: the number of arguments
 *@argv: the array of arguments
 *Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", (argc - 1));
	return (0);
}
